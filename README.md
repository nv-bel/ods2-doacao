# ODS2 - FomeZero

## Rodar o código
1. Instale o PostgreSQL e crie o banco `ods_doacao`

2. Crie uma venv python: `python3 -m ven venv` e ative a venv
3. Instale os requirements dentro da pasta backend `pip install -r backend/requirements.txt`

4. Com a venv ativa, inicie o backend com `python run.py`

5. Em outro terminal, inicie o frontend com `npm run dev`
